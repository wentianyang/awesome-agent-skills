# Change: Enhance Skill PPT

## Why
用户指出 `NanoBanana-PPT-Skills` 包含复杂的代码和依赖，不仅仅是 `SKILL.md`。需要导入完整的项目文件以确保技能可用。

## What Changes
- **Update Skill**: 将 `skills/ppt/` 更新为 `NanoBanana-PPT-Skills` 的完整副本（包含 `requirements.txt`, `src` 等）。
- **Update Spec**: 更新 `skill-ppt` 能够描述代码执行能力（可选）。

## Impact
- **Affected specs**: `skill-ppt` (MODIFIED).
- **Affected code**: `skills/ppt/`.
